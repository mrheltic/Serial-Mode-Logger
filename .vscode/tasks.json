{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install dependencies",
            "type": "shell",
            "command":  "pip install --upgrade pip | pip install pyserial | pip install numpy | pip install matplotlib | pip install scipy",
            "problemMatcher": []
        }

    ]
}